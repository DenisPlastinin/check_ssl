# Foobar
```check_ssl.sh
you may check the SSL certificate for ___ days (default 10 days)
$check-ssls.sh 20
$check-ssls.sh
list sites from list_of_sites.list
```
## Usage
```
$./check-ssls.sh 100
SSL for marketion.ru Certificate will expire less 100 days NOT GOOD
SSL for tut.by is GOOD
SSL for google.com Certificate will expire less 100 days NOT GOOD
SSL for www.altoros.com is GOOD
```
